interface OptionsAxiosReturn {
    host: string;
    port: number;
    auth?: {
        username: string;
        password: string;
    };
    protocol?: string;
}
interface OptionsNodeTunnelReturn {
    host: string;
    port: number;
    proxyAuth?: string;
}
interface OptionsDefaultReturn {
    protocol: string;
    host: string;
    port: string;
    login: string;
    password: string;
}
declare function splitProxy(proxy: string, options?: {
    mode?: 'default';
}): OptionsDefaultReturn;
declare function splitProxy(proxy: string, options?: {
    mode?: 'axios';
}): OptionsAxiosReturn;
declare function splitProxy(proxy: string, options?: {
    mode?: 'node-tunnel';
}): OptionsNodeTunnelReturn;
export default splitProxy;
